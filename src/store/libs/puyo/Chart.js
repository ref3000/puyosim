class Chart {
  constructor () {
    this.chart = []
  }
  set (pos, x, dir) {
    if (obj == null) console.log('error: chart.set, obj is null')
    
  }
  size () {
    return this.chart.length
  }
  clearAfter (pos) {
  } 
}

export default Chart
